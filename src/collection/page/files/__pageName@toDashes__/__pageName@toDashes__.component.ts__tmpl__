<%= docBlock %>import { Component, OnInit } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: '<%= appSelectorPrefix ? (appSelectorPrefix + '-') : '' %><%= toDashes(pageName) %>',
  templateUrl: './<%= toDashes(pageName) %>.component.html',
  styleUrls: ['./<%= toDashes(pageName) %>.component.scss']
})
export class <%= classify(pageName) %>PageComponent implements OnInit {
  public constructor() {
  }

  public ngOnInit(): void {
    // TODO
  }
}
