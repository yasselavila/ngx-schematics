<%= docBlock %>import { Pipe, PipeTransform } from '@angular/core';

@Pipe({ name: '<%= toDashes(pipeName) %>' })
export class <%= classify(pipeName) %>Pipe implements PipeTransform {
  public constructor() {
  }

  public transform(value: any, ...args: any[]): any {
    // TODO
  }
}
